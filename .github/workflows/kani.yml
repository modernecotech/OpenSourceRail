name: kani

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  kani-interlocking:
    name: Kani — osr-interlocking safety harnesses
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Install Kani
        uses: model-checking/kani-github-action@v1
        with:
          working-directory: .
          args: "-p osr-interlocking"

  kani-atp-expired-authority:
    name: Kani — ATP rejects expired movement authority
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Prove the release-selected ATP property
        uses: model-checking/kani-github-action@v1
        with:
          working-directory: .
          args: "-p osr-atp --harness kani_a2_expired_ma_trips"
