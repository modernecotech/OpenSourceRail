# Generated design-system artifacts

This folder is generated by:

```bash
scripts/design-iterate.sh
```

The JSON and Markdown outputs record the best feasible rolling-stock
candidate inside the declared design space, plus ranked shortlists for
review. They are planning artefacts that guide which candidate should be
promoted into FreeCAD/FEM, not homologated design releases.
