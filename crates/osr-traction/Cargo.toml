[package]
name = "osr-traction"
description = "OpenSourceRail — SIL-4 VVVF traction supervisor (torque clamp, anti-slip, inverter gating)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
