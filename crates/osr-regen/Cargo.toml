[package]
name = "osr-regen"
description = "OpenSourceRail — SIL-2 regen arbiter (pack vs trackside storage vs dump resistor)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
