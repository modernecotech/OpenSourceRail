[package]
name = "osr-lighting"
description = "OpenSourceRail — SIL-0 lighting controller (interior, exterior, emergency egress)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
