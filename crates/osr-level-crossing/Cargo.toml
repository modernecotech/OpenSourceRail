[package]
name = "osr-level-crossing"
description = "OpenSourceRail — SIL-4 level-crossing controller (barriers + lights + warning)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
