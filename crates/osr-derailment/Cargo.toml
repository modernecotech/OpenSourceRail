[package]
name = "osr-derailment"
description = "OpenSourceRail — SIL-4 derailment detector (lateral accel + tilt, dual sensor)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
