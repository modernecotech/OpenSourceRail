[package]
name = "osr-historian"
description = "OpenSourceRail — SIL-0 time-series historian (ring-buffer per metric, decimation)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
