[package]
name = "osr-interlocking"
description = "OpenSourceRail — SIL-4 Movement Authority computer and rail state machine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
osr-core = { path = "../osr-core" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
serde_json = "1"
