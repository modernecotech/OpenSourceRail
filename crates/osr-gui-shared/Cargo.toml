[package]
name = "osr-gui-shared"
description = "OpenSourceRail — egui-based network rendering shared across the sim + OCC GUIs (RFC 0018)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
osr-core = { path = "../osr-core" }
egui = "0.29"
