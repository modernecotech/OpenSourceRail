[package]
name = "osr-event-recorder"
description = "OpenSourceRail — SIL-2 onboard event recorder (crash-survivable ring buffer)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
