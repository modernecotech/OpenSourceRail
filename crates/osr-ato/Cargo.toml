[package]
name = "osr-ato"
description = "OpenSourceRail — SIL-2 Automatic Train Operation (speed controller, station approach)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
