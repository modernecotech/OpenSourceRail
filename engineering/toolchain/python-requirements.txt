# Python analysis tools adopted by docs/engineering-design-simulation-plan.md.
# Update deliberately, then run tools/automation/engineering-toolchain.sh --install-python.
ifcopenshell==0.8.5
ifctester==0.8.5
bcf-client==0.8.5
# IfcOpenShell schedule recalculation and EXPRESS validation import these at
# runtime even when the caller is not running a Python test suite.
networkx==3.6.1
pytest==9.1.1
jupedsim==1.4.2
numba==0.66.0
openseespy==3.8.0.0
# 3.5.4 requires SciPy 1.18 while PyBaMM 26.7.1 explicitly excludes 1.18.0.
# Keep the last compatible release until that upstream resolver gap closes.
pandapower==3.5.3
pvlib==0.15.2
pybamm==26.7.1.0
pyswmm==2.1.0
swmm-toolkit==0.17.0
