# Shared runtime for IFC generation, scheduling, validation, IDS and BCF.
ifcopenshell==0.8.5
ifctester==0.8.5
bcf-client==0.8.5
# IfcOpenShell imports these from schedule and EXPRESS validation paths.
networkx==3.6.1
pytest==9.1.1
